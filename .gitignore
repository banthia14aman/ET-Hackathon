node_modules/
dist/
.DS_Store
*.log
tsconfig.tsbuildinfo

# Secrets — never commit a real API key. .env.example is the only tracked env file.
.env
.env.*
!.env.example
.vercel

# Video b-roll pack — regenerate with scratchpad/capture-broll.py (large 4K PNGs)
video-assets/*.png
